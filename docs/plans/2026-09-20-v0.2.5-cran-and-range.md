# cncleanr 0.2.5 implementation plan

## Step 1: Correct parser edge cases

- Output: quantity and range parsers reject malformed digit spacing; ranges
  containing missing endpoints return structured problems.
- Test: focused testthat cases for `1 2`, `3-NA`, and `NA-3`.

## Step 2: Batch range parsing

- Output: unique quantities and all candidate endpoints are parsed in batches
  and expanded to the original rows.
- Test: all existing tests remain unchanged and a 10,000-row benchmark improves
  materially without adding timing assertions to the test suite.

## Step 3: Prepare 0.2.5 metadata and documentation

- Output: DESCRIPTION, NEWS, bilingual README files, LICENSE.md,
  .Rbuildignore, and cran-comments.md form a consistent CRAN candidate.
- Test: DCF parsing, version-reference search, spelling, and source archive
  inspection.

## Step 4: Add a manual-building CI path

- Output: Ubuntu R-release installs TinyTeX and checks the PDF manual; the
  remaining matrix entries keep their existing lightweight check.
- Test: workflow syntax inspection and a successful five-job Actions run.

## Step 5: Verify and publish the candidate branch state

- Output: tested commits pushed to main without modifying v0.2.4 or including
  local editor files.
- Test: local suites, source build, CRAN check, remote CI, and local/remote SHA
  equality.
